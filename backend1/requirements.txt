# domain1 — video extraction & scoring (metrics/rom + creativity)
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
fastdtw>=0.3.4
opencv-python>=4.9.0
mediapipe>=0.10.31,<0.11
pydantic>=2.6.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0

# Audio (creativity / optional align)
librosa>=0.10.0

# YOLO11 — optional; set use_yolo=True in pipeline config
# ultralytics>=8.3.0
