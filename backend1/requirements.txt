# domain1 — video extraction & talent scoring
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
opencv-python>=4.9.0
pydantic>=2.6.0

# MediaPipe legacy solutions (mp.solutions.pose) — 0.10.31+ 에서 solutions 제거됨
# [solutions] extra 는 PyPI에 없음 → 버전 상한으로 고정
mediapipe>=0.10.14,<0.10.31

# API (optional local server)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# YOLO11 track (isolation pipeline/tracker.py)
ultralytics>=8.2.0
lap>=0.5.12

# isolation — 기준 영상 다운로드
yt-dlp>=2024.1.0
